<h1>Create a Song</h1>

<%= form_for @song  do |f| %>
  <%= f.label :title %><br>
  <%= f.text_field :title %><br>

  <%= f.label :artist %><br>
  <%= f.text_field :artist_name %><br>

  <%= f.label :genre%><br>
  <%= f.collection_select :genre_id, Genre.all, :id, :name, :include_blank => true%>

  <%= fields_for @song.notes do |note_b|%>
  <%= note_b.text_field :song[:notes][] %>
  <%= note_b.text_field :song[:notes][] %>
  <%= note_b.text_field :song[:notes][] %>

  <%end%>

  <br />

  <%= f.submit %>
<% end %>


